import React from "react";
const RightBox = ({
  tipAmount,
  total,
  people,
  handleBill,
  handleTip,
  handlePeople,
}) => {
  return <div>
    {/* Add tip Amount and total here */}
    {/* Add button to RESET */}
  </div>;
};

export default RightBox;
